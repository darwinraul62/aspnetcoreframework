using System;
using Autofac;

namespace Ecubytes.Identity.Login.Web.Infraestructure.AutofacModules
{
    public class HttpServicesModule : Autofac.Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);

            
        }
    }
}
